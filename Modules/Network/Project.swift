import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.library(name: "Network",
                              platform: .iOS,
                              dependencies: [.support])
